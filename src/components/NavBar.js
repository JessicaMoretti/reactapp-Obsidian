const NavBar = () => {
    return(
        <nav>
            <a href="#">INICIO</a>
            <a href="#">PRODUCTOS</a>
            <a href="#">ACCESORIOS</a>
        </nav>
    );
}

export default NavBar;